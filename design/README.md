# CityLake Web Design

This folder is the home for the web app's design assets.

## Status

The design referenced in `tasks.md` (`https://api.anthropic.com/v1/design/h/egi7ViY4c47oQcOIO_Xo9w`)
could not be fetched by the agent due to a harness restriction on external URLs that were
not pre-authorised for the session. Drop the design JSON / Figma export / Anthropic Design
manifest into this folder when available — the recommended filename is `spec.json`.

Until the design is in place, the implementation should follow the feature plan in
[`PLAN.md`](./PLAN.md) and use Shadcn UI defaults for visual styling.

## Files

| File | Purpose |
| --- | --- |
| `PLAN.md` | Architecture + feature breakdown for the web app |
| `spec.json` _(to add)_ | Original design manifest (see above) |
