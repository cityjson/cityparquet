# Rust
/target/
**/*.rs.bk
**/*.swp
**/*.swo
Cargo.lock

# IDEs and editors
.idea/
.vscode/
*.iml
*.sublime-workspace
*.sublime-project

# OS generated files
.DS_Store
Thumbs.db

# By-products
*.log
*.tmp
*.bak

# Test output
/coverage/

# Misc
*.old
*.orig
*.rej

# Ignore backup files
*~

# Ignore by default, but track if explicitly added!
!/.gitkeep

.env
.env.keys
design/

# DuckLake runtime state — created on `just api` / `cargo run`, in the crate
# root. Anchored, so it does not also swallow the test fixtures in tests/data.
/metadata.ducklake
/metadata.ducklake.wal
/data/